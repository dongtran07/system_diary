<!-- the comments -->
<div class="comment-<%= comment.id%>">
  <hr>
  <!--<% if(current_user.id == comment.user_id)%>
    <%= link_to "×", comment_path(comment), :method => :delete, :remote => true, :confirm => "Are you sure you want to remove this comment?", :disable_with => "×", :class => 'close'%>
    <%end%>-->
  <h3>
    <%= comment.user.name %>
      <small> <%= "Commented "+time_ago_in_words(comment.created_at)+" ago" unless comment.created_at.blank? %></small>
  </h3>  
  <p><%=simple_format(comment.body)%></p>
</div>
